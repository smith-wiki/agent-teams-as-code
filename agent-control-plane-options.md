# The control plane needs three durable loops outside every guest

**Finding:** no reviewed product documents a turnkey **repository/APM or Nix → Microsandbox → Kubernetes or bare metal → provider accounts** controller. APM materializes locked packages into a supported harness, while `apm runtime` installs AI CLI binaries; neither is an account or workload controller ([install](https://microsoft.github.io/apm/reference/cli/install/), [runtime](https://microsoft.github.io/apm/reference/cli/runtime/)). Nixpkgs can build an [OCI runtime container](https://nixos.org/manual/nixpkgs/stable/#ex-ociTools-buildContainer-bash), and Microsandbox can boot standard OCI images and pin their resolved layers, but Microsandbox documents host-side `create`, `start`, `stop` and `remove` lifecycle rather than an APM or Kubernetes operator ([images](https://docs.microsandbox.dev/images/overview), [lifecycle](https://docs.microsandbox.dev/sandboxes/lifecycle)).

## Documented boundaries

| Product | What it supplies — and what it does not |
|---|---|
| **Kubernetes / GitOps** | A controller watches desired state and reports observed state; a `Job` runs a finite Pod and may retry it. `Lease` objects can elect one controller leader, but neither Jobs nor Leases provide message exactly-once semantics ([controllers](https://kubernetes.io/docs/concepts/architecture/controller/), [Jobs](https://kubernetes.io/docs/concepts/workloads/controllers/job/), [Leases](https://kubernetes.io/docs/concepts/architecture/leases/)). Flux Receivers and Argo CD webhooks accelerate a Git refresh; their documented base behavior remains reconciliation or polling, not provider-event dispatch ([Flux receiver](https://fluxcd.io/flux/components/notification/receivers/), [Flux source interval](https://fluxcd.io/flux/components/source/gitrepositories/), [Argo CD](https://argo-cd.readthedocs.io/en/stable/operator-manual/webhook/)). |
| **Crossplane / External Secrets Operator (ESO)** | Crossplane can manage only resource kinds implemented by an installed provider. ESO fetches or generates values into a Kubernetes `Secret` and can refresh them periodically or on change; it does not thereby create a Slack app, mailbox or model-provider account ([providers](https://docs.crossplane.io/latest/packages/providers/), [ESO refresh](https://external-secrets.io/latest/api/externalsecret/)). Kubernetes Secrets also need encryption at rest and narrow RBAC because they are unencrypted in etcd by default ([Secrets](https://kubernetes.io/docs/concepts/configuration/secret/)). |
| **Existing agent operators** | kagent already reconciles agent runtimes on **Agent Substrate**, not Microsandbox; its 0.x and 1.x APIs differ materially (below). The Agentic Layer and Kagenti operators are real additional reconcilers, but their documented objects create/enrol Kubernetes workloads rather than a portable guest or provider account (comparison below). Rossoctl injects workload identity into existing Deployments, while SIG Apps Agent Sandbox manages Pod-based sandboxes; none of these APIs describes a Microsandbox controller or external SaaS-account lifecycle ([Rossoctl](https://github.com/rossoctl/operator#readme), [Agent Sandbox](https://github.com/kubernetes-sigs/agent-sandbox#readme)). |
| **Provider APIs** | Provisioning is possible only where the provider exposes a suitable admin API and the operator already has tenant authority. Slack supports manifest create/update/delete with a short-lived app-configuration token; Google Workspace exposes user creation, with mailbox licensing and setup still separate. Zulip documents admin-only user creation subject to plan/permission, while its bot-creation help flow is through the UI, so these identities must not be conflated ([Slack](https://docs.slack.dev/app-manifests/configuring-apps-with-app-manifests/#manifest_apis), [Google](https://developers.google.com/workspace/admin/directory/v1/guides/manage-users), [Zulip user API](https://zulip.com/api/create-user), [Zulip bot flow](https://zulip.com/help/add-a-bot-or-integration)). Billing, licensing, approvals and initial tenant-admin authority remain out of band. |

## Real alternatives beyond kagent

The strongest **deployed, reconciled runtime resource** found is AWS CloudFormation's [`AWS::BedrockAgentCore::Runtime`](https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-bedrockagentcore-runtime.html): it declaratively owns an artifact, IAM role, network, environment, authorizer and protocol (`HTTP`, `MCP`, `A2A` or `AGUI`) in AWS's serverless AgentCore runtime, whose contract permits any framework and model. That is broad code portability, not a portable agent definition: the template has no prompt, skills, provider-account or arbitrary sandbox field, and its execution target is AgentCore rather than Microsandbox.

| Candidate and actual input | What is really reconciled | Portability and documented gap |
|---|---|---|
| **Agentic Layer Agent Runtime Operator**, Kubernetes `runtime.agentic-layer.ai/v1alpha1 Agent` YAML | A live operator owns `Agent` and related CRDs ([README](https://github.com/agentic-layer/agent-runtime-operator#readme)). The [`AgentSpec`](https://github.com/agentic-layer/agent-runtime-operator/blob/main/api/v1alpha1/agent_types.go) has instruction, model, A2A sub-agents, MCP tools, image, replicas, resources, volumes and `env`/`envFrom`; its framework enum is exactly `google-adk`, `msaf` or `custom`. This is the richest open, agent-shaped reconciled schema reviewed beyond kagent. | `custom` makes the **container** framework-portable, but the output remains a Kubernetes Deployment/Service. `replicas: 0` is accepted; the schema documents no event source that wakes it. `envFrom` consumes existing Secret/ConfigMap data, not SaaS identities. There is no Microsandbox runtime field. The project had [9 stars and no forks](https://github.com/agentic-layer/agent-runtime-operator) when checked, so this is implementation evidence, not popularity evidence. |
| **Kagenti Operator**, Kubernetes `agent.kagenti.dev/v1alpha1 AgentRuntime` plus an ordinary Deployment | The current API enrols a referenced Deployment/StatefulSet, injects identity/auth/observability sidecars, and discovers A2A metadata in `AgentCard`; it does not define the agent's prompt or tools ([API](https://github.com/red-hat-data-services/agents-operator/blob/main/kagenti-operator/docs/api-reference.md), [operator overview](https://github.com/red-hat-data-services/agents-operator#readme)). | It is framework-neutral only because the user supplies an A2A-compatible Kubernetes workload. Its managed identity is SPIFFE/Keycloak workload identity, not a Slack app, mailbox or model account. No event-to-cold-start or Microsandbox field appears in these two CRDs. |
| **LangSmith Deployment**, `langgraph.json` at build time and image URL/environment at control-plane deployment time | `langgraph.json` names Python/JavaScript dependencies and graph entry points; arbitrary frameworks must be wrapped behind a LangGraph graph interface ([application contract](https://docs.langchain.com/langsmith/application-structure)). In self-hosted mode a listener and operator provision Agent Servers from control-plane revisions ([deployment flow](https://docs.langchain.com/langsmith/deploy-with-control-plane)). | Source logic can wrap Claude Agent SDK, CrewAI, AutoGen and others, but the production reconciliation contract is a LangSmith Agent Server image on Kubernetes, not a portable agent file. The standalone documentation explicitly warns that scale-to-zero can lose tasks or fail to scale up ([limit](https://docs.langchain.com/langsmith/deploy-standalone-server)). It accepts environment variables/API keys, not account-creation intent, and does not target Microsandbox. |
| **AgentSpec / IntentLang**, `.ias` source | This is a real DSL and toolchain: `.ias` composes prompts, skills, MCP, models, policies and a `deploy ... target "kubernetes"` block; its CLI implements `validate`, `plan`, idempotent `apply`, CRD generation and an operator ([source and syntax](https://github.com/szaher/agentspec#readme)). | It is the closest single-file counterexample, but its own architecture converts `.ias` into project-specific CRDs; the operator does not establish `.ias` as an ecosystem API. Secrets are references such as `env(API_KEY)`, and documented targets do not include provider-account provisioning, event wake-up or Microsandbox. The repository had [zero stars and forks](https://github.com/szaher/agentspec) when checked, so it is not evidence of an adopted standard. |

Two near-matches sharpen “account” and “operator.” The [LiteLLM Operator](https://github.com/PalenaAI/litellm-operator#readme) really does reconcile LiteLLM organizations, teams, users and virtual keys through the gateway API, including key rotation/revocation and generated Kubernetes Secrets. Those are **gateway-local identities**; its provider credential CRD consumes a pre-existing API-key Secret, so this disproves “operators never provision accounts” but does not provision a model-vendor, Slack or mailbox identity. AOF publishes unusually close `Agent`, `Fleet` and event-routing `Trigger` CRD schemas, including Slack, GitHub and schedules, but its own page labels the controller the **future AOF Operator** and installation “Future”; the Slack example also takes pre-existing `SLACK_BOT_TOKEN` and `SLACK_APP_TOKEN` ([AOF CRDs](https://docs.aof.sh/docs/reference/kubernetes-crds/)). It is a useful design lead, not a currently reconciled candidate.

These findings reject the stronger hypothesis that no declarative agent reconciler exists. They support the narrower gap: none of the reviewed **documented input APIs** combines a portable agent definition with per-agent external SaaS identity lifecycle, durable provider-event wake-up, and Microsandbox placement. Those are three separate control-plane contracts, not fields that can safely be inferred from “agent deployment.”

Popular coding-agent files do not close that gap. [`AGENTS.md`](https://agents.md/) is unconstrained Markdown guidance and [Codex loads it into each run's instruction chain](https://developers.openai.com/codex/guides/agents-md); [`CLAUDE.md`](https://docs.anthropic.com/en/docs/claude-code/memory) is session context, explicitly not enforced configuration; Copilot [custom-agent profiles](https://docs.github.com/en/copilot/reference/custom-agents-configuration) select prompt, tools and MCP servers inside Copilot; and [`SKILL.md`](https://agentskills.io/specification) packages instructions and optional resources for a compatible client. Their documented consumers are agent clients, not any of the reconcilers above. A future controller may deliberately compile one of them, but existing adoption as an instruction format is not evidence of an operational API.

## kagent if the runtime can change

The [0.10.x `AgentHarness`](https://kagent.dev/docs/kagent/0.x/concepts/agent-harness/) manages **OpenClaw or Hermes** on Agent Substrate and consumes pre-created Slack token Secrets ([example](https://kagent.dev/docs/kagent/0.x/examples/agent-harness/)). The separate [1.0 alpha API](https://kagent.dev/docs/kagent/1.x/about/core-concepts/) replaces the older `Agent`/`AgentHarness` approach with a **`Harness`** CRD (runtime, digest-pinned image, worker pool, secret references) and **`AgentTemplate`** CRD (model, prompt, tools). Its controller compiles the pair into an immutable Substrate `ActorTemplate`; a caller creates a conversation as an `AgentInstance` through kagent's gRPC API, with state in PostgreSQL rather than a Kubernetes CRD. The old `AgentHarness` was removed, **not renamed** to `Harness` ([release notes](https://kagent.dev/docs/kagent/1.x/reference/release-notes/1.0/#removed-and-replaced-resources)).

The 1.x `Harness` supports `kagent`, `codex`, `claude` and `byo` runtimes. A Nix-built `byo` image must implement kagent's A2A **gRPC service on port 80** and an HTTP `/readyz` on 8081; an image containing only a coding-agent CLI is insufficient ([BYO contract](https://kagent.dev/docs/kagent/1.x/agents/bring-your-own-agent/)). Substrate suspends actors at turn boundaries and resumes on traffic. The [detailed snapshot contract](https://kagent.dev/docs/kagent/1.x/substrate-runtime/suspend-and-resume/) says kagent commits `Data` snapshots and cold-boots on resume, discarding process RAM; the [BYO guide](https://kagent.dev/docs/kagent/1.x/agents/bring-your-own-agent/) instead claims RAM survives. Until verified against a pinned release, persist conversation state rather than rely on RAM. Its generated workers use **gVisor**, even though Substrate has a separate microVM class; this is neither Microsandbox nor a documented selectable microVM kagent backend ([sandbox classes](https://kagent.dev/docs/kagent/1.x/substrate-runtime/sandboxing/)).

**Tradeoff:** on Kubernetes, accepting Agent Substrate/gVisor and the current alpha surface replaces much of the custom sandbox/session controller, but **not** provider-account onboarding, existing-secret bootstrap, or external event intake and durable delivery. Bare metal without Kubernetes and a hard Microsandbox requirement still need the proposed controller below. Protect kagent's 1.x API behind a trusted boundary: its [open-source gRPC authenticator/authorizer](https://kagent.dev/docs/kagent/1.x/substrate-runtime/identity/#the-kagent-plane) do not verify caller identity or enforce authorization, according to its own documentation. The [1.x version guide](https://kagent.dev/docs/kagent/1.x/reference/versions/) describes alpha2 while the [latest GitHub release](https://github.com/kagent-dev/kagent/releases/tag/v1.0.0-alpha3) is alpha3; pin documentation and compatibility to the actual release before adoption.

## Proposed contract

The repository group is the **only complete membership list**. Each membership entry carries an opaque, committed `agentId` (for example a UUID) that is never derived from a display name or path; renames therefore preserve identity. Generated operational records contain only that ID, the source revision, descriptor digest, immutable OCI image digest, runtime limits, requested capabilities and provider-specific secret references. They do not copy the whole group.

The image recipe may use Nix directly or run APM during the build. That choice ends at one contract: for each `agentId`, the builder publishes a `BuildRecord(sourceRevision, descriptorDigest, imageDigest)`. A revision cannot become runnable until its descriptor digest is paired with the registry-returned OCI manifest/index `sha256:` digest; a Nix store hash is not that digest, and tags are never accepted by the dispatcher ([OCI descriptors](https://github.com/opencontainers/image-spec/blob/main/descriptor.md)).

## Three-loop flow

1. **Build:** a Git push that changes membership, an agent descriptor, APM lock state or Nix inputs starts CI. The builder materializes the harness, builds and publishes the OCI image, then records its digest. It neither provisions accounts nor starts an agent.
2. **Reconcile configuration and accounts:** a verified Git webhook queues the new revision; digest publication, a credential-rotation event and a periodic full resync queue the same idempotent reconcile key. The reconciler validates unique `agentId` values, compares desired provider bindings with observed remote IDs, calls only supported provider admin APIs, and writes `Ready` for the exact `(source revision, descriptor digest, image digest)` tuple.
3. **Accept events:** provider webhooks or pollers validate the provider identity and insert a durable inbox row keyed by provider event ID and `agentId`. This is separate from GitOps refresh.
4. **Dispatch a session:** a dispatcher claims a renewable per-message lease and, when ordering matters, a per-conversation mutex. It pins the current `Ready` tuple on the session row and cold-starts a uniquely named Microsandbox guest from that digest. The host adapter persists the sandbox’s opaque stable ID before issuing commands. After an uncertain create, it lists/gets and adopts only an exact session-ID, image-digest and provider-binding match; a retry otherwise fails closed ([handle identity](https://docs.microsandbox.dev/sdk/python/sandbox#sandboxhandle)).
5. **Commit before acknowledgement:** the runner durably writes the provider cursor and result/outbox, then attempts to stop and remove the guest, records `Succeeded`, `Failed` or `CleanupBlocked`, and only then acknowledges the inbox event. Provider APIs without idempotency still imply at-least-once external delivery; the outbox can detect and surface ambiguity but cannot manufacture exactly-once behavior.

Microsandbox’s host proxy can substitute a secret only for allowed endpoints while keeping the value out of the guest, but the real value still exists in trusted host memory. Raw SDK values also persist in host-side sandbox configuration, so the dispatcher should resolve that agent’s capability-scoped secret references just in time, use host-environment references and narrow host allowlists, and remove the sandbox after the session ([secret boundary](https://docs.microsandbox.dev/security/secrets)).

## Placement and state ownership

| Process | Exact start/trigger | Bare metal | Kubernetes | Durable owner |
|---|---|---|---|---|
| **Image builder/publisher** | One finite run per relevant Git revision or explicit rebuild | CI runner or a systemd oneshot | Build `Job` in a separate builder pool | OCI registry plus append-only `BuildRecord`; never the guest |
| **Configuration/account reconciler** | Starts at host/cluster boot and stays running; Git/digest/rotation notifications enqueue work; timer rechecks all agents for drift | Supervised systemd service, or Nomad `service` job | Operator `Deployment`; one active reconciler elected with a Kubernetes `Lease` | Desired membership in Git; sparse observed status and immutable provider IDs in a transactional control-plane DB, or CR `status` on Kubernetes |
| **Event/session dispatcher** | Starts at boot; consumes only authenticated provider events or configured polling results | systemd or Nomad `service` job | `Deployment`; replicas claim rows in the shared database | Inbox, leases, pinned session revision, conversation cursor, result and outbox in PostgreSQL (SQLite is acceptable only for a single host); large payloads/results in object storage |
| **Session runner** | Starts only after a message lease is won; exits after result commit or timeout | `systemd-run` transient unit or dispatched Nomad `batch` job | One `Job` on a KVM-capable worker; the Job Pod is the host-side adapter that creates the Microsandbox guest, not the guest itself | No authoritative state; it checkpoints through the dispatcher store |
| **Credentials** | Created/rotated only by an authorized provider adapter or human gate | Vault/cloud secret manager; the service receives a reference or short-lived value | Same external store; ESO may project an already-authorized value into a narrowly scoped Secret | Secret manager owns values and versions; the operational record stores only a provider-specific reference and rotation metadata |

Systemd directly supervises long-running and transient services; Nomad supplies service and batch schedulers and rescheduling policies ([systemd service](https://www.freedesktop.org/software/systemd/man/latest/systemd.service.html), [Nomad jobs](https://developer.hashicorp.com/nomad/docs/job-specification/job), [rescheduling](https://developer.hashicorp.com/nomad/docs/job-specification/reschedule)). On Kubernetes, Job retries make the database lease, unique provider event key and durable outbox mandatory rather than optional. The Microsandbox host adapter and all control-plane code stay outside the ephemeral guest in both placements; Kubernetes nodes must follow the repository’s [KVM-capable deployment path](microsandbox-kubernetes.md).

## Fail-closed reconciliation rules

- **Provider onboarding:** every adapter declares `Observe`, `Create`, `Update`, `Rotate`, `Disable` and `Delete` support independently. Where a Crossplane provider implements the resource, bind it to an explicit provider account configuration rather than an implicit default. Missing API, missing admin scope, interactive consent, licensing or approval sets `ManualActionRequired`; the corresponding capability remains unavailable and the dispatcher refuses events that require it. Secret synchronization is not evidence that the account exists.
- **Credentials:** give every `agentId` a distinct remote app/account where the provider permits it, a least-privilege workload principal and a distinct secret path. Rotation writes a new secret version, proves the new credential, switches the reference and only then revokes the old one. Failure keeps the binding not-ready rather than falling back to a group credential.
- **Conflicts and adoption:** duplicate `agentId`, one remote ID claimed by two agents, an image/descriptor mismatch, or a remote object whose ownership cannot be proven is `Conflict` and blocks execution. Adoption requires an explicit immutable remote ID plus an administrative approval and successful observation; never adopt by display-name matching.
- **Drift:** reconcile only fields the adapter declares authoritative and mutable. Unsupported or immutable drift becomes a condition for human resolution; do not silently delete and recreate. Crossplane likewise treats declared fields as authoritative but does not replace an external resource merely because an immutable field changed ([managed resources](https://docs.crossplane.io/latest/managed-resources/managed-resources/)).
- **Deletion:** absence from an invalid or not-yet-built revision is never a delete signal. A committed tombstone first disables event intake and drains or expires session leases. Revoke credentials, then delete a remote object only when policy is `Delete`, the adapter supports deletion, and the stored ID plus provider-side ownership marker both match; otherwise retain it as `Orphaned`. Keep the tombstone—and, on Kubernetes, its finalizer—until observation confirms absence; acceptance of an asynchronous delete request is not completion ([finalizers](https://kubernetes.io/docs/concepts/overview/working-with-objects/finalizers/)).
